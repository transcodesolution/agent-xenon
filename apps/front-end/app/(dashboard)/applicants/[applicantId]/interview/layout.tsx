import { InterviewRoundsLayout } from './_components/InterviewRoundsLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <InterviewRoundsLayout>{children}</InterviewRoundsLayout>;
}